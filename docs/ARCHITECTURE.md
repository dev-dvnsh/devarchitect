# Architecture

## Overview

Devarchitect is built in Node.js using Commander.js for CLI parsing.
Each command lives in its own file under `src/commands/`.
Project state is stored as versioned JSON files in `.devarchitect/`.

## Project Structure

## Data Flow

## Tech Stack
